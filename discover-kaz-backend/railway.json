{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py loaddata seed_data.json && gunicorn discover_kaz_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
}